# ════════════════════════════════════════════════════════════════
# FOLLOWUS — Environment Variables
# Copy this file to .env and fill in your own values.
# NEVER commit your real .env file to git.
# ════════════════════════════════════════════════════════════════

# ── MongoDB ───────────────────────────────────────────────────
# Your MongoDB Atlas connection string (or local MongoDB URI)
# Format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/
MONGO_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/

# ── Auth / JWT ────────────────────────────────────────────────
# Any long random secret used to sign JWT tokens (keep this private)
JWT_SECRET=change_me_to_a_long_random_secret

# ── App URL ───────────────────────────────────────────────────
# Public base URL of your app (no trailing slash)
# Local dev:  http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# ── AWS S3 (Profile Image Uploads) ───────────────────────────
# Create an IAM user in AWS with S3 permissions and paste here
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=YOUR_S3_BUCKET_NAME

# ── Google OAuth (Sign in with Google) ───────────────────────
# Create OAuth 2.0 credentials at console.cloud.google.com
# Authorized redirect URI: <NEXTAUTH_URL>/api/auth/callback/google
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# ── NextAuth ──────────────────────────────────────────────────
# Any long random secret (openssl rand -base64 32)
NEXTAUTH_SECRET=change_me_to_a_long_random_secret
# Must match your app URL exactly (no trailing slash)
NEXTAUTH_URL=http://localhost:3000

# ── Groq / AI (optional — used for AI features) ──────────────
# Get your API key at console.groq.com
SECRET_KEY=YOUR_GROQ_API_KEY
